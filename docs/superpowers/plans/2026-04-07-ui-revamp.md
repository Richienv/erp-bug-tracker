# UI Revamp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Revamp `bug_tracker.html` to a clean Notion/Claude-aesthetic light-mode UI with a top nav, minimal table rows, and a side panel for bug detail.

**Architecture:** Single HTML file — all CSS and JS embedded. No build step, no frameworks. Changes are surgical edits to the CSS, HTML structure, and JS render/state logic. All storage and data logic remains untouched.

**Tech Stack:** Vanilla HTML/CSS/JS, Inter + JetBrains Mono (Google Fonts), `window.storage` API for persistence.

---

## File Map

| File | Changes |
|---|---|
| `bug_tracker.html` | All changes — CSS vars, layout, components, JS render logic |

---

## Task 1: Replace CSS — Tokens, Fonts, Reset

**Files:**
- Modify: `bug_tracker.html` (lines 6–31)

- [ ] **Step 1: Replace the `<link>` font import (line 6)**

Replace:
```html
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:wght@400;500;600&display=swap" rel="stylesheet">
```
With:
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
```

- [ ] **Step 2: Replace the `:root` block (lines 8–31)**

Replace:
```css
  :root {
    --bg: #0f1117;
    --surface: #1a1d27;
    --surface2: #222535;
    --border: #2e3148;
    --text: #e8eaf0;
    --muted: #6b7280;
    --accent: #6366f1;
    --accent-dim: #312e81;
    --red: #ef4444;
    --red-dim: #3b1515;
    --amber: #f59e0b;
    --amber-dim: #3b2a0a;
    --blue: #3b82f6;
    --blue-dim: #0f2347;
    --green: #22c55e;
    --green-dim: #0f2e1a;
    --gray: #6b7280;
    --gray-dim: #1f2937;
    --mono: 'IBM Plex Mono', monospace;
    --sans: 'IBM Plex Sans', sans-serif;
    --radius: 8px;
    --radius-lg: 12px;
  }
```
With:
```css
  :root {
    --bg: #faf9f7;
    --surface: #f2f0ec;
    --border: #e8e5df;
    --text: #1a1a1a;
    --muted: #8c8a85;
    --accent: #d97706;
    --accent-light: #fef3c7;
    --white: #ffffff;
    --mono: 'JetBrains Mono', monospace;
    --sans: 'Inter', sans-serif;
    --radius: 8px;
    --radius-lg: 12px;
  }
```

- [ ] **Step 3: Replace the `body` rule (line 33)**

Replace:
```css
  body { background: var(--bg); color: var(--text); font-family: var(--sans); font-size: 14px; min-height: 100vh; }
```
With:
```css
  body { background: var(--bg); color: var(--text); font-family: var(--sans); font-size: 14px; min-height: 100vh; -webkit-font-smoothing: antialiased; }
```

- [ ] **Step 4: Open `bug_tracker.html` in a browser. Verify the page background is warm off-white (`#faf9f7`) and text is dark. Fonts may not apply yet since the HTML structure still references old classes — that's expected.**

---

## Task 2: Replace CSS — Old Layout (Remove Sidebar/Shell)

**Files:**
- Modify: `bug_tracker.html` (CSS section — Layout, Sidebar, Topbar, Stats blocks)

- [ ] **Step 1: Replace the `/* Layout */` block**

Replace:
```css
  /* Layout */
  .shell { display: flex; height: 100vh; overflow: hidden; }
  .sidebar { width: 220px; background: var(--surface); border-right: 1px solid var(--border); display: flex; flex-direction: column; flex-shrink: 0; }
  .main { flex: 1; display: flex; flex-direction: column; overflow: hidden; }
```
With:
```css
  /* Layout */
  .app { display: flex; flex-direction: column; height: 100vh; overflow: hidden; }
  .main { flex: 1; display: flex; flex-direction: column; overflow: hidden; }
```

- [ ] **Step 2: Replace the entire `/* Sidebar */` block**

Replace:
```css
  /* Sidebar */
  .brand { padding: 20px 16px 16px; border-bottom: 1px solid var(--border); }
  .brand-name { font-family: var(--mono); font-size: 13px; font-weight: 500; color: var(--accent); letter-spacing: 0.5px; }
  .brand-sub { font-size: 11px; color: var(--muted); margin-top: 2px; }
  .nav { padding: 12px 8px; flex: 1; overflow-y: auto; }
  .nav-section { font-size: 10px; font-weight: 600; color: var(--muted); text-transform: uppercase; letter-spacing: 1px; padding: 8px 8px 4px; }
  .nav-item { display: flex; align-items: center; justify-content: space-between; padding: 7px 10px; border-radius: var(--radius); cursor: pointer; margin-bottom: 1px; font-size: 13px; color: var(--muted); transition: all 0.15s; }
  .nav-item:hover { background: var(--surface2); color: var(--text); }
  .nav-item.active { background: var(--accent-dim); color: var(--accent); }
  .nav-count { font-family: var(--mono); font-size: 10px; background: var(--surface2); padding: 1px 6px; border-radius: 999px; }
  .nav-item.active .nav-count { background: var(--accent); color: #fff; }
  .sidebar-footer { padding: 12px 16px; border-top: 1px solid var(--border); font-size: 11px; color: var(--muted); }
```
With:
```css
  /* Top Nav */
  .topnav { display: flex; align-items: center; gap: 0; padding: 0 24px; height: 52px; border-bottom: 1px solid var(--border); background: var(--white); flex-shrink: 0; position: sticky; top: 0; z-index: 10; }
  .brand { font-size: 15px; font-weight: 600; color: var(--text); margin-right: 20px; white-space: nowrap; letter-spacing: -0.2px; }
  .nav-tabs { display: flex; align-items: center; gap: 2px; flex: 1; }
  .nav-tab { display: inline-flex; align-items: center; gap: 6px; padding: 6px 11px; border: none; background: none; font-family: var(--sans); font-size: 13px; color: var(--muted); cursor: pointer; border-radius: var(--radius); border-bottom: 2px solid transparent; transition: color 0.12s; height: 52px; }
  .nav-tab:hover { color: var(--text); }
  .nav-tab.active { color: var(--accent); border-bottom-color: var(--accent); font-weight: 500; }
  .tab-count { font-size: 11px; background: var(--surface); color: var(--muted); padding: 1px 6px; border-radius: 999px; font-family: var(--sans); }
  .nav-tab.active .tab-count { background: var(--accent-light); color: var(--accent); }
  .nav-right { display: flex; align-items: center; gap: 8px; margin-left: auto; }
```

- [ ] **Step 3: Replace the `/* Topbar */` block**

Replace:
```css
  /* Topbar */
  .topbar { display: flex; align-items: center; gap: 12px; padding: 14px 24px; border-bottom: 1px solid var(--border); background: var(--surface); flex-shrink: 0; }
  .topbar-title { font-weight: 600; font-size: 15px; flex: 1; }
  .search-wrap { display: flex; align-items: center; gap: 8px; background: var(--bg); border: 1px solid var(--border); border-radius: var(--radius); padding: 0 12px; flex: 0 0 260px; }
  .search-wrap input { background: none; border: none; outline: none; color: var(--text); font-size: 13px; padding: 8px 0; width: 100%; font-family: var(--sans); }
  .search-wrap input::placeholder { color: var(--muted); }
  .filters { display: flex; gap: 8px; }
  select { background: var(--bg); color: var(--text); border: 1px solid var(--border); border-radius: var(--radius); padding: 7px 10px; font-size: 12px; font-family: var(--sans); outline: none; cursor: pointer; }
  select:focus { border-color: var(--accent); }
  .btn { display: inline-flex; align-items: center; gap: 6px; padding: 8px 14px; border-radius: var(--radius); font-size: 13px; font-weight: 500; cursor: pointer; border: 1px solid transparent; font-family: var(--sans); transition: all 0.15s; white-space: nowrap; }
  .btn-primary { background: var(--accent); color: #fff; }
  .btn-primary:hover { background: #b45309; }
  .btn-ghost { background: none; color: var(--muted); border-color: var(--border); }
  .btn-ghost:hover { color: var(--text); background: var(--surface); }
  .btn-danger { background: none; color: #dc2626; border-color: #fecaca; }
  .btn-danger:hover { background: #fff1f1; }
  .btn-sm { padding: 5px 10px; font-size: 12px; }
```
With:
```css
  /* Search + Controls */
  .search-wrap { display: flex; align-items: center; gap: 6px; border: 1px solid var(--border); border-radius: var(--radius); padding: 0 10px; background: var(--white); }
  .search-wrap input { background: none; border: none; outline: none; color: var(--text); font-size: 13px; padding: 6px 0; width: 170px; font-family: var(--sans); }
  .search-wrap input::placeholder { color: var(--muted); }
  select { background: var(--white); color: var(--text); border: 1px solid var(--border); border-radius: var(--radius); padding: 6px 10px; font-size: 13px; font-family: var(--sans); outline: none; cursor: pointer; }
  select:focus { border-color: var(--accent); }
  .btn { display: inline-flex; align-items: center; gap: 6px; padding: 7px 14px; border-radius: var(--radius); font-size: 13px; font-weight: 500; cursor: pointer; border: 1px solid transparent; font-family: var(--sans); transition: all 0.12s; white-space: nowrap; }
  .btn-primary { background: var(--accent); color: #fff; }
  .btn-primary:hover { background: #b45309; }
  .btn-ghost { background: none; color: var(--muted); border-color: var(--border); }
  .btn-ghost:hover { color: var(--text); background: var(--surface); }
  .btn-danger { background: none; color: #dc2626; border-color: #fecaca; }
  .btn-danger:hover { background: #fff1f1; }
  .btn-sm { padding: 4px 10px; font-size: 12px; }
```

- [ ] **Step 4: Replace the `/* Stats row */` block**

Replace:
```css
  /* Stats row */
  .stats { display: grid; grid-template-columns: repeat(5, 1fr); gap: 12px; padding: 16px 24px; border-bottom: 1px solid var(--border); background: var(--surface); flex-shrink: 0; }
  .stat { background: var(--bg); border: 1px solid var(--border); border-radius: var(--radius); padding: 12px 14px; }
  .stat-val { font-family: var(--mono); font-size: 24px; font-weight: 500; line-height: 1; }
  .stat-lbl { font-size: 11px; color: var(--muted); margin-top: 4px; }
  .c-total { color: var(--text); }
  .c-open { color: var(--red); }
  .c-prog { color: var(--amber); }
  .c-fix { color: var(--blue); }
  .c-ver { color: var(--green); }
```
With:
```css
  /* (Stats row removed — counts live in nav tabs) */
```

- [ ] **Step 5: Open browser. The page will look broken — expected at this stage since HTML still has old structure. Just verify no CSS syntax errors (page shouldn't be completely blank white).**

---

## Task 3: Replace CSS — Bug Table

**Files:**
- Modify: `bug_tracker.html` (CSS section — Bug list block)

- [ ] **Step 1: Replace the `/* Bug list */` block**

Replace:
```css
  /* Bug list */
  .list-area { flex: 1; overflow-y: auto; padding: 16px 24px; }
  .list-header { display: grid; grid-template-columns: 90px 1fr 120px 80px 100px; gap: 12px; padding: 8px 14px; font-size: 11px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; border-bottom: 1px solid var(--border); margin-bottom: 8px; }
  .bug-row { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-lg); margin-bottom: 6px; overflow: hidden; transition: border-color 0.15s; }
  .bug-row:hover { border-color: #3e4266; }
  .bug-row.expanded { border-color: var(--accent); }
  .bug-summary { display: grid; grid-template-columns: 90px 1fr 120px 80px 100px; gap: 12px; padding: 12px 14px; align-items: center; cursor: pointer; }
  .bug-id { font-family: var(--mono); font-size: 11px; color: var(--muted); }
  .bug-title-cell { }
  .bug-title-text { font-size: 13px; font-weight: 500; color: var(--text); }
  .bug-module { font-size: 11px; color: var(--muted); margin-top: 2px; }
  .badge { display: inline-flex; align-items: center; gap: 5px; padding: 3px 10px; border-radius: 999px; font-size: 11px; font-weight: 500; font-family: var(--mono); white-space: nowrap; }
  .badge::before { content: ''; display: inline-block; width: 5px; height: 5px; border-radius: 50%; }
  .s-Open { background: var(--red-dim); color: var(--red); }
  .s-Open::before { background: var(--red); }
  .s-InProgress { background: var(--amber-dim); color: var(--amber); }
  .s-InProgress::before { background: var(--amber); }
  .s-Fixed { background: var(--blue-dim); color: var(--blue); }
  .s-Fixed::before { background: var(--blue); }
  .s-Verified { background: var(--green-dim); color: var(--green); }
  .s-Verified::before { background: var(--green); }
  .s-Closed { background: var(--gray-dim); color: var(--gray); }
  .s-Closed::before { background: var(--gray); }
  .bug-reporter { font-size: 12px; color: var(--muted); }
  .bug-date { font-family: var(--mono); font-size: 11px; color: var(--muted); }
  .img-pill { display: inline-flex; align-items: center; gap: 3px; font-size: 10px; color: var(--blue); background: var(--blue-dim); padding: 2px 7px; border-radius: 999px; margin-top: 3px; }
```
With:
```css
  /* Bug table */
  .table-wrap { flex: 1; overflow-y: auto; }
  .table-header { display: grid; grid-template-columns: 80px 1fr 130px 90px 110px 20px; gap: 12px; padding: 9px 28px; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; color: var(--muted); border-bottom: 1px solid var(--border); background: var(--bg); position: sticky; top: 0; }
  .bug-row { display: grid; grid-template-columns: 80px 1fr 130px 90px 110px 20px; gap: 12px; padding: 11px 28px; border-bottom: 1px solid var(--border); align-items: center; cursor: pointer; transition: background 0.1s; }
  .bug-row:hover { background: var(--surface); }
  .bug-id { font-family: var(--mono); font-size: 11px; color: var(--muted); }
  .bug-title-text { font-size: 13px; font-weight: 500; color: var(--text); }
  .bug-module { font-size: 11px; color: var(--muted); margin-top: 2px; }
  .badge { display: inline-flex; align-items: center; padding: 2px 8px; border-radius: 999px; font-size: 11px; font-weight: 500; white-space: nowrap; }
  .s-Open { background: #fff1f1; color: #dc2626; }
  .s-InProgress { background: #fffbeb; color: #d97706; }
  .s-Fixed { background: #eff6ff; color: #2563eb; }
  .s-Verified { background: #f0fdf4; color: #16a34a; }
  .s-Closed { background: #f5f5f5; color: #6b7280; }
  .bug-reporter { font-size: 12px; color: var(--muted); }
  .bug-date { font-size: 11px; color: var(--muted); font-family: var(--mono); }
  .bug-attach { color: var(--muted); display: flex; align-items: center; }
```

- [ ] **Step 2: Replace the `/* Detail panel */` block (`.bug-detail`, `.detail-grid`, `.detail-section`, `.detail-lbl`, `.detail-val`, `.imgs-row`, `.img-box`, `.img-box-lbl`, `.img-frame`, `.img-frame img`, `.img-empty`, `.detail-actions`, `.del-confirm`)**

Replace:
```css
  /* Detail panel */
  .bug-detail { border-top: 1px solid var(--border); padding: 16px 20px; background: var(--bg); }
  .detail-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
  .detail-section { }
  .detail-lbl { font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.8px; color: var(--muted); margin-bottom: 6px; }
  .detail-val { font-size: 13px; color: var(--text); line-height: 1.6; white-space: pre-wrap; }
  .detail-val.empty { color: var(--muted); font-style: italic; }
  .imgs-row { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 16px; padding-top: 16px; border-top: 1px solid var(--border); }
  .img-box { }
  .img-box-lbl { font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.8px; color: var(--muted); margin-bottom: 6px; }
  .img-frame { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; min-height: 100px; display: flex; align-items: center; justify-content: center; }
  .img-frame img { width: 100%; height: auto; display: block; cursor: zoom-in; max-height: 280px; object-fit: contain; }
  .img-empty { font-size: 11px; color: var(--muted); padding: 20px; }
  .detail-actions { display: flex; gap: 8px; margin-top: 16px; padding-top: 16px; border-top: 1px solid var(--border); flex-wrap: wrap; align-items: center; }
  .del-confirm { background: var(--red-dim); border: 1px solid var(--red); border-radius: var(--radius); padding: 10px 14px; font-size: 12px; color: var(--red); display: flex; align-items: center; gap: 8px; flex-wrap: wrap; margin-top: 8px; }
```
With:
```css
  /* Side panel */
  .panel-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,0.15); z-index: 40; }
  .side-panel { position: fixed; top: 0; right: 0; width: 420px; height: 100vh; background: var(--white); border-left: 1px solid var(--border); z-index: 50; display: flex; flex-direction: column; transform: translateX(100%); transition: transform 0.2s ease; }
  .side-panel.open { transform: translateX(0); }
  .panel-header { display: flex; align-items: center; justify-content: space-between; padding: 14px 20px; border-bottom: 1px solid var(--border); flex-shrink: 0; }
  .panel-id { font-family: var(--mono); font-size: 11px; color: var(--muted); }
  .panel-close { background: none; border: none; font-size: 18px; color: var(--muted); cursor: pointer; line-height: 1; padding: 2px 4px; font-family: var(--sans); }
  .panel-close:hover { color: var(--text); }
  .panel-body { flex: 1; overflow-y: auto; padding: 20px; }
  .panel-title { font-size: 17px; font-weight: 600; color: var(--text); margin-bottom: 8px; line-height: 1.4; }
  .panel-module { display: inline-block; font-size: 11px; background: var(--surface); color: var(--muted); padding: 2px 9px; border-radius: 999px; margin-bottom: 20px; }
  .panel-section { margin-bottom: 18px; }
  .panel-lbl { font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.8px; color: var(--muted); margin-bottom: 6px; }
  .panel-val { font-size: 13px; color: var(--text); line-height: 1.6; white-space: pre-wrap; }
  .panel-val.empty { color: var(--muted); font-style: italic; }
  .panel-imgs { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  .panel-img-lbl { font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.8px; color: var(--muted); margin-bottom: 6px; }
  .panel-img-frame { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; min-height: 80px; display: flex; align-items: center; justify-content: center; }
  .panel-img-frame img { width: 100%; height: auto; max-height: 200px; object-fit: contain; cursor: zoom-in; }
  .panel-img-empty { font-size: 11px; color: var(--muted); padding: 16px; }
  .panel-actions { padding: 12px 20px; border-top: 1px solid var(--border); display: flex; gap: 8px; flex-shrink: 0; flex-wrap: wrap; align-items: center; }
  .del-confirm { width: 100%; background: #fff1f1; border: 1px solid #fecaca; border-radius: var(--radius); padding: 8px 12px; font-size: 12px; color: #dc2626; display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
```

---

## Task 4: Replace CSS — Empty State, Modal, Misc

**Files:**
- Modify: `bug_tracker.html` (CSS section)

- [ ] **Step 1: Replace the `/* Empty state */` block**

Replace:
```css
  /* Empty state */
  .empty-state { text-align: center; padding: 60px 20px; color: var(--muted); }
  .empty-state-icon { font-size: 40px; margin-bottom: 12px; opacity: 0.4; }
  .empty-state-text { font-size: 15px; margin-bottom: 6px; color: var(--text); }
  .empty-state-sub { font-size: 13px; }
```
With:
```css
  /* Empty state */
  .empty-state { text-align: center; padding: 80px 20px; color: var(--muted); }
  .empty-state-icon { font-size: 32px; margin-bottom: 12px; opacity: 0.3; }
  .empty-state-text { font-size: 15px; margin-bottom: 4px; color: var(--text); font-weight: 500; }
  .empty-state-sub { font-size: 13px; }
```

- [ ] **Step 2: Replace the `/* Modal overlay */` block**

Replace:
```css
  /* Modal overlay */
  .overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.7); display: flex; align-items: flex-start; justify-content: center; z-index: 100; padding: 40px 20px; overflow-y: auto; }
  .modal { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-lg); width: 100%; max-width: 640px; padding: 24px; }
  .modal-title { font-size: 16px; font-weight: 600; margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center; }
  .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
  .form-group { }
  .form-group.full { grid-column: 1/-1; }
  .form-group label { display: block; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; color: var(--muted); margin-bottom: 5px; }
  .form-group input, .form-group select, .form-group textarea { width: 100%; background: var(--bg); color: var(--text); border: 1px solid var(--border); border-radius: var(--radius); padding: 9px 12px; font-size: 13px; font-family: var(--sans); outline: none; transition: border-color 0.15s; }
  .form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--accent); }
  .form-group textarea { resize: vertical; min-height: 80px; line-height: 1.5; }
  .img-upload-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  .upload-zone { border: 1px dashed var(--border); border-radius: var(--radius); padding: 12px; text-align: center; transition: border-color 0.15s; }
  .upload-zone:hover { border-color: var(--accent); }
  .upload-zone-lbl { font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; color: var(--muted); margin-bottom: 8px; }
  .upload-zone img { width: 100%; max-height: 140px; object-fit: contain; border-radius: 6px; margin-bottom: 6px; }
  .upload-zone input[type=file] { font-size: 11px; color: var(--muted); width: 100%; }
  .modal-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 20px; padding-top: 16px; border-top: 1px solid var(--border); }
```
With:
```css
  /* Modal overlay */
  .overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.25); display: flex; align-items: flex-start; justify-content: center; z-index: 100; padding: 48px 20px; overflow-y: auto; }
  .modal { background: var(--white); border-radius: var(--radius-lg); width: 100%; max-width: 560px; padding: 28px; box-shadow: 0 8px 32px rgba(0,0,0,0.10); }
  .modal-title { font-size: 16px; font-weight: 600; color: var(--text); margin-bottom: 24px; display: flex; justify-content: space-between; align-items: center; }
  .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .form-group { }
  .form-group.full { grid-column: 1/-1; }
  .form-group label { display: block; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; color: var(--muted); margin-bottom: 5px; }
  .form-group input, .form-group select, .form-group textarea { width: 100%; background: var(--white); color: var(--text); border: 1px solid var(--border); border-radius: var(--radius); padding: 8px 12px; font-size: 13px; font-family: var(--sans); outline: none; transition: border-color 0.15s; }
  .form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-light); }
  .form-group textarea { resize: vertical; min-height: 80px; line-height: 1.5; }
  .img-upload-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  .upload-zone { border: 1px dashed var(--border); border-radius: var(--radius); padding: 12px; text-align: center; transition: border-color 0.15s; background: var(--bg); }
  .upload-zone:hover { border-color: var(--accent); }
  .upload-zone-lbl { font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; color: var(--muted); margin-bottom: 8px; }
  .upload-zone img { width: 100%; max-height: 140px; object-fit: contain; border-radius: 6px; margin-bottom: 6px; }
  .upload-zone input[type=file] { font-size: 11px; color: var(--muted); width: 100%; }
  .modal-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 24px; padding-top: 16px; border-top: 1px solid var(--border); }
```

- [ ] **Step 3: Replace the `/* Lightbox */` block**

Replace:
```css
  /* Lightbox */
  .lightbox { position: fixed; inset: 0; background: rgba(0,0,0,0.9); display: flex; align-items: center; justify-content: center; z-index: 200; cursor: zoom-out; }
  .lightbox img { max-width: 90vw; max-height: 90vh; border-radius: 8px; }
```
With:
```css
  /* Lightbox */
  .lightbox { position: fixed; inset: 0; background: rgba(0,0,0,0.85); display: flex; align-items: center; justify-content: center; z-index: 200; cursor: zoom-out; }
  .lightbox img { max-width: 90vw; max-height: 90vh; border-radius: var(--radius); box-shadow: 0 24px 64px rgba(0,0,0,0.4); }
```

- [ ] **Step 4: Replace the tail of the CSS (scrollbar, misc)**

Replace:
```css
  /* Scrollbar */
  ::-webkit-scrollbar { width: 6px; height: 6px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }

  .hidden { display: none !important; }
  .divider { width: 1px; height: 20px; background: var(--border); }
```
With:
```css
  /* Scrollbar */
  ::-webkit-scrollbar { width: 5px; height: 5px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }

  .hidden { display: none !important; }
```

---

## Task 5: Replace HTML — Body Structure

**Files:**
- Modify: `bug_tracker.html` (HTML body)

- [ ] **Step 1: Replace the entire `<body>` content (everything between `<body>` and `<!-- New/Edit Modal -->`)**

Replace:
```html
<div class="shell">
  <!-- Sidebar -->
  <div class="sidebar">
    <div class="brand">
      <div class="brand-name">ERP // BUGS</div>
      <div class="brand-sub">Richie · Darren · Raymond</div>
    </div>
    <nav class="nav">
      <div class="nav-section">Filter by status</div>
      <div class="nav-item active" onclick="setStatusFilter('')" id="nav-all">All bugs <span class="nav-count" id="cnt-all">0</span></div>
      <div class="nav-item" onclick="setStatusFilter('Open')" id="nav-open">Open <span class="nav-count" id="cnt-open">0</span></div>
      <div class="nav-item" onclick="setStatusFilter('In Progress')" id="nav-prog">In Progress <span class="nav-count" id="cnt-prog">0</span></div>
      <div class="nav-item" onclick="setStatusFilter('Fixed')" id="nav-fix">Fixed <span class="nav-count" id="cnt-fix">0</span></div>
      <div class="nav-item" onclick="setStatusFilter('Verified')" id="nav-ver">Verified <span class="nav-count" id="cnt-ver">0</span></div>
      <div class="nav-item" onclick="setStatusFilter('Closed')" id="nav-closed">Closed <span class="nav-count" id="cnt-clo">0</span></div>

      <div class="nav-section" style="margin-top:12px">Filter by reporter</div>
      <div class="nav-item" onclick="setReporterFilter('')" id="nav-r-all">All reporters</div>
      <div class="nav-item" onclick="setReporterFilter('Richie')" id="nav-r-richie">Richie</div>
      <div class="nav-item" onclick="setReporterFilter('Darren')" id="nav-r-darren">Darren</div>
      <div class="nav-item" onclick="setReporterFilter('Raymond')" id="nav-r-raymond">Raymond</div>
    </nav>
    <div class="sidebar-footer">Finance Module QA v2</div>
  </div>

  <!-- Main -->
  <div class="main">
    <!-- Topbar -->
    <div class="topbar">
      <div class="topbar-title" id="topbar-label">All Bugs</div>
      <div class="search-wrap">
        <svg width="13" height="13" fill="none" stroke="#6b7280" stroke-width="2" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
        <input type="text" id="search-input" placeholder="Search bugs..." oninput="applyFilters()">
      </div>
      <div class="filters">
        <select id="filter-module" onchange="applyFilters()">
          <option value="">All modules</option>
          <option>Finance / Invoice</option><option>Finance / COA</option><option>Finance / Journal</option>
          <option>Finance / AR Aging</option><option>Finance / AP Aging</option><option>Finance / Bank Recon</option>
          <option>Finance / Petty Cash</option><option>Finance / Debit Note</option><option>Finance / Credit Note</option>
          <option>Finance / Credit Limit</option><option>Inventory</option><option>Purchase</option>
          <option>Sales</option><option>HR</option><option>Settings</option><option>Dashboard</option><option>Auth</option>
        </select>
      </div>
      <div class="divider"></div>
      <button class="btn btn-primary" onclick="openForm()">+ New Bug</button>
    </div>

    <!-- Stats -->
    <div class="stats">
      <div class="stat"><div class="stat-val c-total" id="s-total">0</div><div class="stat-lbl">Total logged</div></div>
      <div class="stat"><div class="stat-val c-open" id="s-open">0</div><div class="stat-lbl">Open</div></div>
      <div class="stat"><div class="stat-val c-prog" id="s-prog">0</div><div class="stat-lbl">In progress</div></div>
      <div class="stat"><div class="stat-val c-fix" id="s-fix">0</div><div class="stat-lbl">Fixed</div></div>
      <div class="stat"><div class="stat-val c-ver" id="s-ver">0</div><div class="stat-lbl">Verified</div></div>
    </div>

    <!-- List -->
    <div class="list-area">
      <div class="list-header">
        <span>ID</span><span>Title / Module</span><span>Status</span><span>Reporter</span><span>Date</span>
      </div>
      <div id="bug-list"></div>
    </div>
  </div>
</div>
```
With:
```html
<div class="app">
  <!-- Top Nav -->
  <nav class="topnav">
    <div class="brand">ERP Bugs</div>
    <div class="nav-tabs">
      <button class="nav-tab active" onclick="setStatusFilter('')" id="nav-all">All <span class="tab-count" id="cnt-all">0</span></button>
      <button class="nav-tab" onclick="setStatusFilter('Open')" id="nav-open">Open <span class="tab-count" id="cnt-open">0</span></button>
      <button class="nav-tab" onclick="setStatusFilter('In Progress')" id="nav-prog">In Progress <span class="tab-count" id="cnt-prog">0</span></button>
      <button class="nav-tab" onclick="setStatusFilter('Fixed')" id="nav-fix">Fixed <span class="tab-count" id="cnt-fix">0</span></button>
      <button class="nav-tab" onclick="setStatusFilter('Verified')" id="nav-ver">Verified <span class="tab-count" id="cnt-ver">0</span></button>
      <button class="nav-tab" onclick="setStatusFilter('Closed')" id="nav-closed">Closed <span class="tab-count" id="cnt-clo">0</span></button>
    </div>
    <div class="nav-right">
      <div class="search-wrap">
        <svg width="13" height="13" fill="none" stroke="#8c8a85" stroke-width="2" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
        <input type="text" id="search-input" placeholder="Search..." oninput="applyFilters()">
      </div>
      <select id="filter-module" onchange="applyFilters()">
        <option value="">All modules</option>
        <option>Finance / Invoice</option><option>Finance / COA</option><option>Finance / Journal</option>
        <option>Finance / AR Aging</option><option>Finance / AP Aging</option><option>Finance / Bank Recon</option>
        <option>Finance / Petty Cash</option><option>Finance / Debit Note</option><option>Finance / Credit Note</option>
        <option>Finance / Credit Limit</option><option>Inventory</option><option>Purchase</option>
        <option>Sales</option><option>HR</option><option>Settings</option><option>Dashboard</option><option>Auth</option>
      </select>
      <button class="btn btn-primary" onclick="openForm()">+ New Bug</button>
    </div>
  </nav>

  <!-- Main -->
  <div class="main">
    <div class="table-wrap">
      <div class="table-header">
        <span>ID</span><span>Title / Module</span><span>Status</span><span>Reporter</span><span>Date</span><span></span>
      </div>
      <div id="bug-list"></div>
    </div>
  </div>
</div>

<!-- Side Panel -->
<div class="panel-backdrop hidden" id="panel-backdrop" onclick="closePanel()"></div>
<div class="side-panel" id="side-panel">
  <div class="panel-header">
    <span class="panel-id" id="panel-bug-id"></span>
    <button class="panel-close" onclick="closePanel()">×</button>
  </div>
  <div class="panel-body" id="panel-body"></div>
  <div class="panel-actions" id="panel-actions"></div>
</div>
```

- [ ] **Step 2: Open browser. Verify: warm off-white background, top nav bar visible with "ERP Bugs" + filter tabs + search + button. Bug list area should be visible (empty or with bugs). Side panel should be off-screen (not visible). Fonts should be Inter.**

---

## Task 6: Update JS — State, renderBugRow, updateStats, filteredBugs

**Files:**
- Modify: `bug_tracker.html` (JS section)

- [ ] **Step 1: Replace the `state` declaration**

Replace:
```js
let state = { bugs: [], filter: { q:'', status:'', reporter:'', module:'' }, expandedId: null, expandedImgs: {}, editId: null, formBefore: null, formAfter: null, confirmDelId: null };
```
With:
```js
let state = { bugs: [], filter: { q:'', status:'', module:'' }, panelBugId: null, panelImgs: null, editId: null, formBefore: null, formAfter: null, confirmDelId: null };
```

- [ ] **Step 2: Replace the `filteredBugs` function to extend search to reporter and remove reporter filter**

Replace:
```js
function filteredBugs(){
  const {q,status,reporter,module}=state.filter;
  return state.bugs.filter(b=>{
    if(q&&!(b.title+' '+b.description+' '+b.id).toLowerCase().includes(q.toLowerCase()))return false;
    if(status&&b.status!==status)return false;
    if(reporter&&b.reportedBy!==reporter)return false;
    if(module&&b.module!==module)return false;
    return true;
  }).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
}
```
With:
```js
function filteredBugs(){
  const {q,status,module}=state.filter;
  return state.bugs.filter(b=>{
    if(q&&!(b.title+' '+b.description+' '+b.id+' '+(b.reportedBy||'')).toLowerCase().includes(q.toLowerCase()))return false;
    if(status&&b.status!==status)return false;
    if(module&&b.module!==module)return false;
    return true;
  }).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
}
```

- [ ] **Step 3: Replace the `updateStats` function (remove old `s-total`, `s-open` etc. references)**

Replace:
```js
function updateStats(){
  const s=getStats();
  document.getElementById('s-total').textContent=s.tot;
  document.getElementById('s-open').textContent=s.open;
  document.getElementById('s-prog').textContent=s.prog;
  document.getElementById('s-fix').textContent=s.fix;
  document.getElementById('s-ver').textContent=s.ver;
  document.getElementById('cnt-all').textContent=s.tot;
  document.getElementById('cnt-open').textContent=s.open;
  document.getElementById('cnt-prog').textContent=s.prog;
  document.getElementById('cnt-fix').textContent=s.fix;
  document.getElementById('cnt-ver').textContent=s.ver;
  document.getElementById('cnt-clo').textContent=s.clo;
}
```
With:
```js
function updateStats(){
  const s=getStats();
  document.getElementById('cnt-all').textContent=s.tot;
  document.getElementById('cnt-open').textContent=s.open;
  document.getElementById('cnt-prog').textContent=s.prog;
  document.getElementById('cnt-fix').textContent=s.fix;
  document.getElementById('cnt-ver').textContent=s.ver;
  document.getElementById('cnt-clo').textContent=s.clo;
}
```

- [ ] **Step 4: Replace the `renderBugRow` function (remove all expand/detail logic, replace with simple grid row)**

Replace the entire `renderBugRow` function:
```js
function renderBugRow(bug){
  const exp=state.expandedId===bug.id;
  const cls=sc(bug.status);
  const imgs=state.expandedImgs[bug.id];
  const nextS={'Open':'In Progress','In Progress':'Fixed','Fixed':'Verified','Verified':'Closed'}[bug.status];
  const isDel=state.confirmDelId===bug.id;

  let html=`<div class="bug-row${exp?' expanded':''}" id="row-${bug.id}">
    <div class="bug-summary" onclick="toggleExpand('${bug.id}')">
      <div class="bug-id">${esc(bug.id)}</div>
      <div class="bug-title-cell">
        <div class="bug-title-text">${esc(bug.title)}</div>
        <div class="bug-module">${esc(bug.module||'—')}</div>
        ${bug.hasBeforeImg||bug.hasAfterImg?'<div class="img-pill"><svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-6-6L5 21"/></svg> screenshots</div>':''}
      </div>
      <div><span class="badge s-${cls}">${bug.status}</span></div>
      <div class="bug-reporter">${esc(bug.reportedBy)}</div>
      <div class="bug-date">${fmt(bug.createdAt)}</div>
    </div>`;

  if(exp){
    html+=`<div class="bug-detail">
      <div class="detail-grid">
        <div class="detail-section">
          <div class="detail-lbl">Description</div>
          <div class="detail-val ${bug.description?'':'empty'}">${bug.description?esc(bug.description):'Not provided'}</div>
        </div>
        <div class="detail-section">
          <div class="detail-lbl">Fix description</div>
          <div class="detail-val ${bug.fixDescription?'':'empty'}">${bug.fixDescription?esc(bug.fixDescription):'Not yet fixed'}</div>
        </div>
      </div>`;

    if(bug.verifiedBy||bug.notes){
      html+=`<div class="detail-grid" style="margin-top:14px">`;
      if(bug.verifiedBy) html+=`<div class="detail-section"><div class="detail-lbl">Verified by</div><div class="detail-val">${esc(bug.verifiedBy)} ${bug.resolvedAt?'· '+fmt(bug.resolvedAt):''}</div></div>`;
      if(bug.notes) html+=`<div class="detail-section"><div class="detail-lbl">Notes</div><div class="detail-val">${esc(bug.notes)}</div></div>`;
      html+=`</div>`;
    }

    if(bug.hasBeforeImg||bug.hasAfterImg){
      html+=`<div class="imgs-row">`;
      if(bug.hasBeforeImg){
        html+=`<div class="img-box"><div class="img-box-lbl">Before</div><div class="img-frame">`;
        html+=imgs&&imgs.before?`<img src="${imgs.before}" onclick="showLightbox('${bug.id}','before')" alt="Before">`:`<div class="img-empty">Loading...</div>`;
        html+=`</div></div>`;
      }
      if(bug.hasAfterImg){
        html+=`<div class="img-box"><div class="img-box-lbl">After</div><div class="img-frame">`;
        html+=imgs&&imgs.after?`<img src="${imgs.after}" onclick="showLightbox('${bug.id}','after')" alt="After">`:`<div class="img-empty">Loading...</div>`;
        html+=`</div></div>`;
      }
      html+=`</div>`;
    }

    html+=`<div class="detail-actions">`;
    if(nextS) html+=`<button class="btn btn-ghost btn-sm" onclick="quickStatus('${bug.id}','${nextS}')">→ Mark "${nextS}"</button>`;
    html+=`<button class="btn btn-ghost btn-sm" onclick="editBug('${bug.id}')">Edit</button>`;
    if(!isDel){
      html+=`<button class="btn btn-danger btn-sm" onclick="confirmDel('${bug.id}')">Delete</button>`;
    }
    html+=`</div>`;
    if(isDel){
      html+=`<div class="del-confirm">Delete ${bug.id} permanently? This cannot be undone.
        <button class="btn btn-danger btn-sm" onclick="doDelete('${bug.id}')">Yes, delete</button>
        <button class="btn btn-ghost btn-sm" onclick="cancelDel()">Cancel</button>
      </div>`;
    }
    html+=`</div>`;
  }
  html+=`</div>`;
  return html;
}
```
With:
```js
function renderBugRow(bug){
  const cls=sc(bug.status);
  const attachIcon=bug.hasBeforeImg||bug.hasAfterImg
    ?`<svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="m21.44 11.05-9.19 9.19a6 6 0 0 1-8.49-8.49l8.57-8.57A4 4 0 1 1 18 8.84l-8.59 8.57a2 2 0 0 1-2.83-2.83l8.49-8.48"/></svg>`
    :'';
  return `<div class="bug-row" id="row-${bug.id}" onclick="openPanel('${bug.id}')">
    <div class="bug-id">${esc(bug.id)}</div>
    <div>
      <div class="bug-title-text">${esc(bug.title)}</div>
      <div class="bug-module">${esc(bug.module||'—')}</div>
    </div>
    <div><span class="badge s-${cls}">${bug.status}</span></div>
    <div class="bug-reporter">${esc(bug.reportedBy)}</div>
    <div class="bug-date">${fmt(bug.createdAt)}</div>
    <div class="bug-attach">${attachIcon}</div>
  </div>`;
}
```

- [ ] **Step 5: Open browser. Verify: bug rows render as clean flat table rows with no expand. Clicking a row does nothing useful yet (openPanel not defined) — that's fine. No JS errors in console for rendering.**

---

## Task 7: Add JS — openPanel, closePanel, renderPanelContent

**Files:**
- Modify: `bug_tracker.html` (JS section — replace `// Expand` block)

- [ ] **Step 1: Replace the `// Expand` block (the `toggleExpand` function)**

Replace:
```js
// Expand
async function toggleExpand(id){
  if(state.expandedId===id){state.expandedId=null;state.confirmDelId=null;renderList();return;}
  state.expandedId=id;state.confirmDelId=null;renderList();
  const bug=state.bugs.find(b=>b.id===id);
  if(bug&&(bug.hasBeforeImg||bug.hasAfterImg)&&!state.expandedImgs[id]){
    const imgs=await loadImgs(id);state.expandedImgs[id]=imgs;renderList();
  }
}
```
With:
```js
// Side panel
async function openPanel(id){
  state.panelBugId=id;
  state.confirmDelId=null;
  state.panelImgs=null;
  renderPanel();
  document.getElementById('side-panel').classList.add('open');
  document.getElementById('panel-backdrop').classList.remove('hidden');
  const bug=state.bugs.find(b=>b.id===id);
  if(bug&&(bug.hasBeforeImg||bug.hasAfterImg)){
    const imgs=await loadImgs(id);
    state.panelImgs=imgs;
    renderPanel();
  }
}

function closePanel(){
  state.panelBugId=null;
  state.confirmDelId=null;
  state.panelImgs=null;
  document.getElementById('side-panel').classList.remove('open');
  document.getElementById('panel-backdrop').classList.add('hidden');
}

function renderPanel(){
  const id=state.panelBugId;
  if(!id)return;
  const bug=state.bugs.find(b=>b.id===id);
  if(!bug){closePanel();return;}
  const imgs=state.panelImgs;
  const isDel=state.confirmDelId===id;
  const nextS={'Open':'In Progress','In Progress':'Fixed','Fixed':'Verified','Verified':'Closed'}[bug.status];

  document.getElementById('panel-bug-id').textContent=bug.id;

  let body=`
    <div class="panel-title">${esc(bug.title)}</div>
    ${bug.module?`<div class="panel-module">${esc(bug.module)}</div>`:''}
    <div class="panel-section">
      <div class="panel-lbl">Description</div>
      <div class="panel-val${bug.description?'':' empty'}">${bug.description?esc(bug.description):'Not provided'}</div>
    </div>
    <div class="panel-section">
      <div class="panel-lbl">Fix notes</div>
      <div class="panel-val${bug.fixDescription?'':' empty'}">${bug.fixDescription?esc(bug.fixDescription):'Not yet fixed'}</div>
    </div>`;

  if(bug.verifiedBy){
    body+=`<div class="panel-section">
      <div class="panel-lbl">Verified by</div>
      <div class="panel-val">${esc(bug.verifiedBy)}${bug.resolvedAt?' · '+fmt(bug.resolvedAt):''}</div>
    </div>`;
  }
  if(bug.notes){
    body+=`<div class="panel-section">
      <div class="panel-lbl">Notes</div>
      <div class="panel-val">${esc(bug.notes)}</div>
    </div>`;
  }
  if(bug.hasBeforeImg||bug.hasAfterImg){
    body+=`<div class="panel-section"><div class="panel-lbl">Screenshots</div><div class="panel-imgs">`;
    if(bug.hasBeforeImg){
      body+=`<div><div class="panel-img-lbl">Before</div><div class="panel-img-frame">`;
      body+=imgs&&imgs.before?`<img src="${imgs.before}" onclick="showLightbox('before')" alt="Before">`:`<div class="panel-img-empty">Loading…</div>`;
      body+=`</div></div>`;
    }
    if(bug.hasAfterImg){
      body+=`<div><div class="panel-img-lbl">After</div><div class="panel-img-frame">`;
      body+=imgs&&imgs.after?`<img src="${imgs.after}" onclick="showLightbox('after')" alt="After">`:`<div class="panel-img-empty">Loading…</div>`;
      body+=`</div></div>`;
    }
    body+=`</div></div>`;
  }
  document.getElementById('panel-body').innerHTML=body;

  let actions='';
  if(nextS) actions+=`<button class="btn btn-ghost btn-sm" onclick="quickStatus('${id}','${nextS}')">→ ${esc(nextS)}</button>`;
  actions+=`<button class="btn btn-ghost btn-sm" onclick="editBug('${id}')">Edit</button>`;
  if(!isDel){
    actions+=`<button class="btn btn-danger btn-sm" onclick="confirmDel('${id}')">Delete</button>`;
  } else {
    actions+=`<div class="del-confirm" style="margin-top:0">Delete ${esc(id)} permanently?
      <button class="btn btn-danger btn-sm" onclick="doDelete('${id}')">Yes, delete</button>
      <button class="btn btn-ghost btn-sm" onclick="cancelDel()">Cancel</button>
    </div>`;
  }
  document.getElementById('panel-actions').innerHTML=actions;
}
```

- [ ] **Step 2: Open browser. Verify: clicking a bug row opens the side panel from the right with a backdrop. Panel shows bug title, description, fix notes. Close button (×) dismisses it. Clicking backdrop also closes it.**

---

## Task 8: Update JS — quickStatus, confirmDel, doDelete, showLightbox, setStatusFilter

**Files:**
- Modify: `bug_tracker.html` (JS section)

- [ ] **Step 1: Replace `quickStatus` to re-render panel after status change**

Replace:
```js
// Status quick change
async function quickStatus(id,ns){
  const bug=state.bugs.find(b=>b.id===id);if(!bug)return;
  bug.status=ns;
  if((ns==='Verified'||ns==='Closed')&&!bug.resolvedAt)bug.resolvedAt=new Date().toISOString().split('T')[0];
  await saveBugs();updateStats();renderList();
}
```
With:
```js
// Status quick change
async function quickStatus(id,ns){
  const bug=state.bugs.find(b=>b.id===id);if(!bug)return;
  bug.status=ns;
  if((ns==='Verified'||ns==='Closed')&&!bug.resolvedAt)bug.resolvedAt=new Date().toISOString().split('T')[0];
  await saveBugs();updateStats();renderList();
  if(state.panelBugId===id)renderPanel();
}
```

- [ ] **Step 2: Replace `confirmDel` and `cancelDel` to use `renderPanel` instead of `renderList`**

Replace:
```js
// Delete
function confirmDel(id){state.confirmDelId=id;renderList();}
function cancelDel(){state.confirmDelId=null;renderList();}
async function doDelete(id){
  state.bugs=state.bugs.filter(b=>b.id!==id);
  if(state.expandedId===id)state.expandedId=null;
  delete state.expandedImgs[id];
  state.confirmDelId=null;
  await delImgs(id);await saveBugs();updateStats();renderList();
}
```
With:
```js
// Delete
function confirmDel(id){state.confirmDelId=id;renderPanel();}
function cancelDel(){state.confirmDelId=null;renderPanel();}
async function doDelete(id){
  state.bugs=state.bugs.filter(b=>b.id!==id);
  state.confirmDelId=null;
  await delImgs(id);await saveBugs();updateStats();renderList();
  closePanel();
}
```

- [ ] **Step 3: Replace `showLightbox` to use `state.panelImgs`**

Replace:
```js
function showLightbox(id,t){
  const imgs=state.expandedImgs[id];if(!imgs)return;
  const src=t==='before'?imgs.before:imgs.after;if(!src)return;
  document.getElementById('lightbox-img').src=src;
  document.getElementById('lightbox').classList.remove('hidden');
}
```
With:
```js
function showLightbox(t){
  const imgs=state.panelImgs;if(!imgs)return;
  const src=t==='before'?imgs.before:imgs.after;if(!src)return;
  document.getElementById('lightbox-img').src=src;
  document.getElementById('lightbox').classList.remove('hidden');
}
```

- [ ] **Step 4: Replace `setStatusFilter` to remove old `topbar-label` reference**

Replace:
```js
function setStatusFilter(s){
  state.filter.status=s;
  // Update nav active
  ['all','open','prog','fix','ver','closed'].forEach(k=>{document.getElementById('nav-'+k)?.classList.remove('active');});
  const map={'':'all','Open':'open','In Progress':'prog','Fixed':'fix','Verified':'ver','Closed':'closed'};
  document.getElementById('nav-'+(map[s]||'all'))?.classList.add('active');
  const labels={'':'All Bugs','Open':'Open Bugs','In Progress':'In Progress','Fixed':'Fixed','Verified':'Verified','Closed':'Closed'};
  document.getElementById('topbar-label').textContent=labels[s]||'All Bugs';
  applyFilters();
}
```
With:
```js
function setStatusFilter(s){
  state.filter.status=s;
  ['all','open','prog','fix','ver','closed'].forEach(k=>{document.getElementById('nav-'+k)?.classList.remove('active');});
  const map={'':'all','Open':'open','In Progress':'prog','Fixed':'fix','Verified':'ver','Closed':'closed'};
  document.getElementById('nav-'+(map[s]||'all'))?.classList.add('active');
  applyFilters();
}
```

- [ ] **Step 5: Remove the `setReporterFilter` function entirely**

Remove:
```js
function setReporterFilter(r){
  state.filter.reporter=r;
  ['all','richie','darren','raymond'].forEach(k=>{document.getElementById('nav-r-'+k)?.classList.remove('active');});
  const map={'':'all','Richie':'richie','Darren':'darren','Raymond':'raymond'};
  document.getElementById('nav-r-'+(map[r]||'all'))?.classList.add('active');
  applyFilters();
}
```

- [ ] **Step 6: Update `applyFilters` to remove `reporter` from filter state**

Replace:
```js
function applyFilters(){
  state.filter.q=document.getElementById('search-input')?.value||'';
  state.filter.module=document.getElementById('filter-module')?.value||'';
  updateStats();
  renderList();
}
```
With:
```js
function applyFilters(){
  state.filter.q=document.getElementById('search-input')?.value||'';
  state.filter.module=document.getElementById('filter-module')?.value||'';
  updateStats();renderList();
}
```

- [ ] **Step 7: Remove stale `expandedImgs` reference from `submitForm`**

Replace (inside `submitForm`, in the `if(state.editId)` block):
```js
    if(i>=0)state.bugs[i]=bug;
    delete state.expandedImgs[bug.id];
```
With:
```js
    if(i>=0)state.bugs[i]=bug;
```

- [ ] **Step 8: Update `submitForm` to refresh panel if the edited bug was open**

After `closeModal();updateStats();renderList();` in `submitForm`, replace:
```js
  closeModal();updateStats();renderList();
```
With:
```js
  const wasPanel=state.panelBugId===bug.id;
  closeModal();updateStats();renderList();
  if(wasPanel){state.panelImgs=null;await openPanel(bug.id);}
```

- [ ] **Step 8: Open browser. Full end-to-end test:**
  - Page loads with warm off-white background, Inter font, top nav tabs
  - Tab counts update correctly
  - Click a bug → side panel slides in from right
  - `→ In Progress` button advances status, panel reflects the change
  - Edit button opens the modal; saving refreshes the panel
  - Delete button shows confirm inline in panel; confirming closes panel and removes bug
  - Search by name (e.g. "Richie") filters the list
  - Module dropdown filters correctly
  - Screenshots (if any) show in panel with lightbox on click
  - `+ New Bug` opens modal, saving adds bug to list

---

## Task 9: Re-add Files to Git and Commit

**Files:**
- `bug_tracker.html` and all other project files currently untracked

- [ ] **Step 1: Stage and commit all project files**

```bash
git add bug_tracker.html fix/ report/ shared/ package.json package-lock.json vercel.json .gitignore docs/plans/
git commit -m "feat: full UI revamp — Notion/Claude light theme, top nav, side panel"
```

Expected output: commit succeeds, all files tracked.

- [ ] **Step 2: Verify**

```bash
git status
```
Expected: `nothing to commit, working tree clean`
